# ElectionDatasetPrepare
A simple Python script to fetch, clean, and prepare election datasets from the Dawn Election Portal  API. The script downloads election data for multiple years, removes unnecessary fields, and saves the final dataset as a JSON file for easy analysis.
